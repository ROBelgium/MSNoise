name,default,definition,type,possible_values
ref_begin,1970-01-01,Start of REF period. Absolute date (YYYY-MM-DD) OR negative integer for rolling-index mode (e.g. -5 means 5 windows before current),str,
ref_end,2100-01-01,End of REF period. Absolute date (YYYY-MM-DD) OR negative integer (e.g. -1 means exclude self). Must be > ref_begin when both are negative.,str,
stack_method,linear,Stack method for REF computation (Mode A only),str,linear/pws
pws_timegate,10.0,"If stack_method=pws: smoothing width in seconds (Mode A only)",float,
pws_power,2.0,"If stack_method=pws: power of the weighting (Mode A only)",float,
export_format,MSEED,Export format for REF stacks (Mode A only),str,SAC/MSEED/BOTH
sac_format,doublets,SAC format for REF stacks (Mode A only),str,doublets/clarke
